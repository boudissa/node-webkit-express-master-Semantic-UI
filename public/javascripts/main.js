$(function() {

    $('.ui.dropdown')
    .dropdown();

    
    // var winh = $(window).height(),
    // upper = $('.upper-bar').innerHeight();

    // $('.side-bar').height(winh - (upper));
    
});